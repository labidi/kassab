{% trans_default_domain 'FOSUserBundle' %}

<form action="{{change_password_submit_url}}" method="post" class="form-horizontal">
	{% if change_password.current_password is defined %}
		{{ form_row(change_password.current_password) }}
	{% endif %}
	{{ form_row(change_password.plainPassword.first) }}
	{{ form_row(change_password.plainPassword.second) }}
	<div class="form-actions">
		<div class="col-md-offset-3">
			<button type="submit" class="btn  green">Modifier le mot de passe</button>
			{# {{-- form_widget(change_password.submit, {'label':'Modifier le mot de passe','attr':{'class':'btn green'}}) }} #}
		</div>
	</div>
</form>
